{
  "appName": "Fanmade Communities",
  "packageId": "com.fanmadeC.roxfox",
  "version": "1.0.0",
  "format": "apk",
  "buildDate": "2026-08-17T23:40:42.907Z",
  "files": [
    {
      "name": "index.html",
      "type": "file",
      "content": "<!DOCTYPE html>\n<html lang=\"pt-BR\">\n<head>\n  <meta charset=\"UTF-8\">\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n  <title>Fanmade Communities com Chat</title>\n  <style>\n    * { box-sizing: border-box; margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, sans-serif; }\n    body { background: #121212; color: #fff; padding: 20px; max-width: 1050px; margin: 0 auto; }\n    \n    header { display: flex; justify-content: space-between; align-items: center; padding-bottom: 20px; border-bottom: 1px solid #333; margin-bottom: 20px; flex-wrap: wrap; gap: 15px; }\n    .user-info { display: flex; align-items: center; gap: 12px; }\n    \n    .user-profile-badge { display: flex; align-items: center; gap: 10px; cursor: pointer; padding: 5px 10px; border-radius: 20px; transition: background 0.2s; }\n    .user-profile-badge:hover { background: #2a2a2a; }\n    img.avatar-header { width: 36px !important; height: 36px !important; min-width: 36px; min-height: 36px; border-radius: 50%; object-fit: cover; border: 2px solid #ff9800; background: #333; display: block; }\n    \n    .admin-badge { background: #ff4757; color: white; font-size: 10px; padding: 2px 6px; border-radius: 4px; font-weight: bold; margin-left: 4px; display: inline-block; text-transform: uppercase; }\n\n    .card { background: #1e1e1e; padding: 20px; border-radius: 8px; margin-bottom: 25px; border: 1px solid #333; }\n    input[type=\"file\"] { display: none; }\n    input[type=\"text\"], input[type=\"password\"], textarea, select { width: 100%; padding: 10px; margin: 8px 0; border-radius: 4px; border: 1px solid #444; background: #2a2a2a; color: #fff; }\n    textarea { resize: vertical; min-height: 70px; }\n    \n    .btn { background: #ff9800; color: #000; padding: 8px 16px; border: none; border-radius: 5px; cursor: pointer; font-weight: bold; display: inline-block; text-align: center; }\n    .btn:hover { background: #e68a00; }\n    .btn-secondary { background: #007bff; color: #fff; }\n    .btn-secondary:hover { background: #0056b3; }\n    .btn-danger { background: #dc3545; color: #fff; }\n    .btn-danger:hover { background: #bd2130; }\n    .btn-upload { background: #28a745; color: #fff; margin-bottom: 10px; }\n    .btn-upload:hover { background: #218838; }\n\n    .avatar-preview-box { display: flex; align-items: center; justify-content: center; gap: 15px; margin: 10px 0; }\n    img.avatar-preview { width: 60px !important; height: 60px !important; min-width: 60px; min-height: 60px; border-radius: 50%; object-fit: cover; background: #333; border: 2px solid #555; display: block; }\n    \n    .communities-nav { display: flex; gap: 10px; overflow-x: auto; padding-bottom: 10px; margin-bottom: 20px; border-bottom: 1px solid #333; }\n    .community-tab { background: #1e1e1e; border: 1px solid #444; padding: 8px 16px; border-radius: 20px; cursor: pointer; white-space: nowrap; font-size: 14px; font-weight: bold; transition: all 0.2s; display: flex; align-items: center; gap: 6px; }\n    .community-tab:hover { background: #2a2a2a; border-color: #ff9800; }\n    .community-tab.active { background: #ff9800; color: #000; border-color: #ff9800; }\n\n    .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 18px; }\n    .post-card { background: #1e1e1e; border-radius: 8px; overflow: hidden; border: 1px solid #333; cursor: pointer; transition: transform 0.2s; display: flex; flex-direction: column; position: relative; }\n    .post-card:hover { transform: scale(1.02); }\n    \n    .post-card img, .post-card video { width: 100%; height: 200px !important; max-height: 200px !important; object-fit: cover; display: block; background: #000; }\n    \n    .post-meta { padding: 12px; font-size: 13px; color: #aaa; text-align: left; display: flex; flex-direction: column; gap: 6px; flex: 1; }\n    .post-author-box { display: flex; align-items: center; gap: 8px; margin-bottom: 4px; justify-content: space-between; }\n    .author-info-left { display: flex; align-items: center; gap: 8px; }\n    img.post-author-avatar { width: 25px !important; height: 25px !important; min-width: 25px; min-height: 25px; border-radius: 50%; object-fit: cover; display: block; }\n    .post-title { font-size: 16px; font-weight: bold; color: #fff; margin-top: 4px; }\n    .post-desc { font-size: 13px; color: #ccc; overflow: hidden; text-overflow: ellipsis; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; }\n    .post-link { color: #00a2ff; text-decoration: none; word-break: break-all; font-size: 12px; }\n    .post-link:hover { text-decoration: underline; }\n    \n    .group-badge { background: #333; color: #ff9800; font-size: 11px; padding: 3px 8px; border-radius: 12px; display: inline-block; font-weight: bold; width: fit-content; margin-bottom: 4px; border: 1px solid #ff980033; }\n\n    .msg { margin-top: 10px; font-weight: bold; font-size: 14px; text-align: center; }\n\n    /* Estilo do Chat integrado */\n    .chat-container { display: flex; flex-direction: column; height: 450px; background: #1a1a1a; border-radius: 8px; border: 1px solid #333; overflow: hidden; }\n    .chat-messages { flex: 1; overflow-y: auto; padding: 15px; display: flex; flex-direction: column; gap: 10px; }\n    .chat-input-area { padding: 10px; background: #222; display: flex; gap: 10px; border-top: 1px solid #333; }\n    .chat-msg { background: #2a2a2a; padding: 10px 14px; border-radius: 12px; max-width: 75%; align-self: flex-start; font-size: 14px; color: #fff; border: 1px solid #444; }\n    .chat-msg.me { background: #ff9800; color: #000; align-self: flex-end; border-color: #e68a00; }\n    .chat-msg-header { display: flex; align-items: center; gap: 6px; margin-bottom: 4px; }\n    img.chat-msg-avatar { width: 20px !important; height: 20px !important; min-width: 20px; min-height: 20px; border-radius: 50%; object-fit: cover; }\n\n    .modal { display: none; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.85); z-index: 1000; justify-content: center; align-items: center; }\n    .modal-content { background: #1e1e1e; border-radius: 8px; max-width: 850px; width: 90%; max-height: 90vh; display: flex; flex-direction: row; overflow: hidden; border: 1px solid #444; }\n    @media (max-width: 650px) { .modal-content { flex-direction: column; overflow-y: auto; } }\n    \n    .modal-media { flex: 1.2; background: #000; display: flex; justify-content: center; align-items: center; max-height: 70vh; }\n    .modal-media img, .modal-media video { max-width: 100%; max-height: 70vh; object-fit: contain; }\n    \n    .modal-details { flex: 1; padding: 20px; display: flex; flex-direction: column; justify-content: space-between; background: #181818; }\n    .modal-author-info { display: flex; align-items: center; gap: 10px; margin-bottom: 12px; }\n    img.modal-avatar { width: 40px !important; height: 40px !important; min-width: 40px; min-height: 40px; border-radius: 50%; object-fit: cover; display: block; }\n    \n    .comments-list { flex: 1; overflow-y: auto; max-height: 200px; margin: 15px 0; border-top: 1px solid #333; border-bottom: 1px solid #333; padding: 10px 0; }\n    .comment-item { font-size: 13px; margin-bottom: 8px; line-height: 1.4; display: flex; align-items: flex-start; justify-content: space-between; gap: 8px; }\n    img.comment-avatar { width: 22px !important; height: 22px !important; min-width: 22px; min-height: 22px; border-radius: 50%; object-fit: cover; display: block; }\n\n    .modal-box { background: #1e1e1e; border-radius: 8px; max-width: 480px; width: 90%; padding: 25px; border: 1px solid #444; text-align: center; }\n    .bio-text { font-size: 12px; color: #aaa; margin-top: -2px; font-style: italic; }\n\n    .tabs { display: flex; gap: 10px; margin-bottom: 15px; }\n    .tab { padding: 8px 16px; background: #2a2a2a; border-radius: 5px; cursor: pointer; font-size: 14px; font-weight: bold; }\n    .tab.active { background: #ff9800; color: #000; }\n\n    .admin-panel-box { border: 2px dashed #ff4757; background: #1a1515; padding: 15px; border-radius: 8px; margin-bottom: 20px; }\n  </style>\n</head>\n<body>\n\n  <header>\n    <h2>🔥 Fanmade Communities</h2>\n    <div id=\"auth-header\" class=\"user-info\">Carregando...</div>\n  </header>\n\n  <!-- PAINEL DE AUTH -->\n  <div id=\"auth-panel\" class=\"card\" style=\"text-align: center;\">\n    <h3 id=\"auth-title\">Entrar no Fanmade Communities</h3>\n    \n    <div id=\"avatar-field\" style=\"display: none;\">\n      <div class=\"avatar-preview-box\">\n        <img id=\"avatar-preview-img\" class=\"avatar-preview\" src=\"https://via.placeholder.com/60/333333/FFFFFF?text=Perfil\" alt=\"Preview\">\n        <label class=\"btn btn-upload\" for=\"avatarInput\">Escolher Foto de Perfil</label>\n        <input type=\"file\" id=\"avatarInput\" accept=\"image/*\" onchange=\"carregarPreviewAvatar(event, 'avatar-preview-img', 'tempRegister')\">\n      </div>\n      <textarea id=\"reg-bio\" placeholder=\"Escreva uma breve bio/descrição...\"></textarea>\n    </div>\n\n    <input type=\"text\" id=\"username\" placeholder=\"Seu Usuário (@nickname)...\">\n    <input type=\"password\" id=\"password\" placeholder=\"Sua Senha...\">\n    <input type=\"password\" id=\"reg-recovery-code\" placeholder=\"Defina sua Senha Única (para recuperação)...\" style=\"display: none;\">\n    <br><br>\n    <button class=\"btn\" id=\"btn-submit\" onclick=\"handleAuth()\">Entrar</button>\n    <div style=\"margin-top: 12px; font-size: 13px; color: #aaa;\">\n      <span id=\"auth-link-text\">Não tem conta?</span> \n      <a href=\"javascript:void(0)\" onclick=\"toggleAuthMode()\" id=\"auth-link\" style=\"color:#ff9800\">Cadastre-se</a>\n      <div id=\"forgot-link-box\" style=\"margin-top: 8px;\">\n        <a href=\"javascript:void(0)\" onclick=\"abrirModalRecuperacao()\" style=\"color:#aaa; font-size:12px;\">Esqueci minha senha</a>\n      </div>\n    </div>\n    <div id=\"auth-msg\" class=\"msg\"></div>\n  </div>\n\n  <!-- PAINEL PRINCIPAL DO USUÁRIO LOGADO -->\n  <div id=\"upload-panel\" class=\"card\" style=\"display: none;\">\n    <div id=\"admin-tools-container\" style=\"display: none;\"></div>\n\n    <div class=\"tabs\">\n      <div class=\"tab active\" id=\"tab-post\" onclick=\"mudarAbaPainel('post')\">📸 / 🎥 Criar Publicação</div>\n      <div class=\"tab\" id=\"tab-grupo\" onclick=\"mudarAbaPainel('grupo')\">👥 Criar Comunidade</div>\n    </div>\n\n    <!-- FORMULARIO DE POST -->\n    <div id=\"form-post\">\n      <p style=\"color: #aaa; margin-bottom: 12px; font-size: 13px;\">Selecione uma imagem ou vídeo para publicar:</p>\n\n      <select id=\"post-group-select\">\n        <option value=\"\">Feed Geral</option>\n      </select>\n\n      <label class=\"btn btn-upload\" for=\"mediaInput\" id=\"label-media\">1. Selecionar Imagem ou Vídeo</label>\n      <input type=\"file\" id=\"mediaInput\" accept=\"image/*,video/*\" onchange=\"validarSelecaoMidia(event)\">\n      <span id=\"media-nome-status\" style=\"font-size: 13px; color: #28a745; margin-left: 10px;\"></span>\n\n      <input type=\"text\" id=\"post-title\" placeholder=\"Título da Publicação...\">\n      <textarea id=\"post-desc\" placeholder=\"Escreva uma legenda / descrição...\"></textarea>\n      <input type=\"text\" id=\"post-link\" placeholder=\"Link relacionado (ex: https://site.com)...\">\n\n      <button class=\"btn\" onclick=\"salvarPost()\" style=\"width: 100%; margin-top: 10px;\">Publicar na Comunidade</button>\n    </div>\n\n    <!-- FORMULARIO DE CRIAR GRUPO -->\n    <div id=\"form-grupo\" style=\"display: none;\">\n      <p style=\"color: #aaa; margin-bottom: 12px; font-size: 13px;\">Crie uma aba de comunidade para reunir posts e mensagens:</p>\n      \n      <div class=\"avatar-preview-box\">\n        <img id=\"group-icon-preview\" class=\"avatar-preview\" src=\"https://via.placeholder.com/60/333/FFFFFF?text=Grupo\" alt=\"Ícone Grupo\">\n        <label class=\"btn btn-upload\" for=\"groupIconInput\">Ícone da Comunidade</label>\n        <input type=\"file\" id=\"groupIconInput\" accept=\"image/*\" onchange=\"carregarPreviewGrupo(event)\">\n      </div>\n\n      <input type=\"text\" id=\"group-name\" placeholder=\"Nome da Comunidade (ex: RPG, Arte, Animes)...\">\n      <textarea id=\"group-desc\" placeholder=\"Sobre o que é esta comunidade?\"></textarea>\n      \n      <label style=\"display: flex; align-items: center; gap: 8px; margin: 12px 0; cursor: pointer; font-size: 14px;\">\n        <input type=\"checkbox\" id=\"group-is-private\" style=\"width: auto;\"> 🔒 Tornar Comunidade Privada (apenas por convite)\n      </label>\n\n      <button class=\"btn btn-secondary\" onclick=\"salvarGrupo()\" style=\"width: 100%; margin-top: 10px;\">Criar Comunidade</button>\n    </div>\n\n    <div id=\"action-msg\" class=\"msg\"></div>\n  </div>\n\n  <!-- ABAS DE NAVEGAÇÃO DE COMUNIDADES -->\n  <div class=\"communities-nav\" id=\"communities-nav-bar\">\n    <div class=\"community-tab active\" onclick=\"filtrarComunidade('')\">🔥 Feed Geral</div>\n  </div>\n\n  <!-- CABEÇALHO DA COMUNIDADE ATIVA + SELETOR POSTS/CHAT -->\n  <div style=\"display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; flex-wrap: wrap; gap: 10px;\">\n    <h3 id=\"current-community-title\" style=\"color: #ff9800; margin: 0;\">Feed Geral</h3>\n    <div id=\"view-mode-selector\" style=\"display: flex; gap: 8px;\">\n      <button class=\"btn\" id=\"btn-mode-posts\" onclick=\"alternarModo('posts')\">🖼️ Posts</button>\n      <button class=\"btn btn-secondary\" id=\"btn-mode-chat\" onclick=\"alternarModo('chat')\">💬 Chat</button>\n    </div>\n  </div>\n\n  <!-- CONTEÚDO PRINCIPAL (GALERIA DE POSTS OU CHAT) -->\n  <div id=\"galeria\" class=\"grid\"></div>\n\n  <div id=\"chat-box\" class=\"chat-container\" style=\"display: none;\">\n    <div class=\"chat-messages\" id=\"chat-messages\"></div>\n    <div class=\"chat-input-area\">\n      <input type=\"text\" id=\"chat-input\" placeholder=\"Digite uma mensagem...\" onkeypress=\"if(event.key === 'Enter') enviarMensagemChat()\">\n      <button class=\"btn\" onclick=\"enviarMensagemChat()\">Enviar</button>\n    </div>\n  </div>\n\n  <!-- MODAL EDITAR PERFIL -->\n  <div id=\"modal-perfil\" class=\"modal\" onclick=\"fecharModalPerfil(event)\">\n    <div class=\"modal-box\" onclick=\"event.stopPropagation()\">\n      <h3>Editar Perfil</h3>\n      <p style=\"font-size: 12px; color: #aaa; margin-bottom: 15px;\">Altere suas informações públicas:</p>\n\n      <div class=\"avatar-preview-box\">\n        <img id=\"edit-avatar-preview\" class=\"avatar-preview\" src=\"\" alt=\"Avatar\">\n        <label class=\"btn btn-upload\" for=\"editAvatarInput\">Mudar Foto</label>\n        <input type=\"file\" id=\"editAvatarInput\" accept=\"image/*\" onchange=\"carregarPreviewAvatar(event, 'edit-avatar-preview', 'tempEdit')\">\n      </div>\n\n      <input type=\"text\" id=\"edit-username\" placeholder=\"Novo nome de usuário (@handle)\">\n      <textarea id=\"edit-bio\" placeholder=\"Sua bio / descrição pessoal...\"></textarea>\n\n      <div style=\"display: flex; gap: 10px; margin-top: 15px;\">\n        <button class=\"btn\" onclick=\"salvarPerfil()\" style=\"flex: 1;\">Salvar</button>\n        <button class=\"btn btn-danger\" onclick=\"document.getElementById('modal-perfil').style.display = 'none'\" style=\"flex: 1;\">Cancelar</button>\n      </div>\n      <div id=\"edit-profile-msg\" class=\"msg\"></div>\n    </div>\n  </div>\n\n  <!-- MODAL RECUPERAR CONTA -->\n  <div id=\"modal-recuperacao\" class=\"modal\" onclick=\"fecharModalRecuperacao(event)\">\n    <div class=\"modal-box\" onclick=\"event.stopPropagation()\">\n      <h3>Recuperar Conta</h3>\n      <p style=\"font-size: 12px; color: #aaa; margin-bottom: 15px;\">Informe seu usuário e sua Senha Única cadastrada para redefinir sua senha.</p>\n      \n      <input type=\"text\" id=\"rec-username\" placeholder=\"Seu usuário (@nick)...\">\n      <input type=\"password\" id=\"rec-code\" placeholder=\"Sua Senha Única (Código de Recuperação)...\">\n      <hr style=\"border: 0; border-top: 1px solid #444; margin: 15px 0;\">\n      <input type=\"password\" id=\"rec-new-pass\" placeholder=\"Nova senha de login...\">\n\n      <div style=\"display: flex; gap: 10px; margin-top: 15px;\">\n        <button class=\"btn\" onclick=\"recuperarConta()\" style=\"flex: 1;\">Redefinir Senha</button>\n        <button class=\"btn btn-danger\" onclick=\"document.getElementById('modal-recuperacao').style.display = 'none'\" style=\"flex: 1;\">Voltar</button>\n      </div>\n      <div id=\"rec-msg\" class=\"msg\"></div>\n    </div>\n  </div>\n\n  <!-- MODAL EXIBIR POST -->\n  <div id=\"modal-post\" class=\"modal\" onclick=\"fecharModalPost(event)\">\n    <div class=\"modal-content\" onclick=\"event.stopPropagation()\">\n      <div class=\"modal-media\" id=\"modal-media-container\"></div>\n      <div class=\"modal-details\">\n        <div>\n          <div id=\"modal-group-badge\"></div>\n          <div class=\"modal-author-info\">\n            <img id=\"modal-author-avatar\" class=\"modal-avatar\" src=\"\" alt=\"Avatar\">\n            <div>\n              <h4 id=\"modal-author-name\">@autor</h4>\n              <p id=\"modal-author-bio\" class=\"bio-text\"></p>\n              <p id=\"modal-date\" style=\"font-size: 11px; color: #888; margin-top: 2px;\"></p>\n            </div>\n          </div>\n          \n          <h3 id=\"modal-title\" style=\"margin-bottom: 6px; color: #fff;\"></h3>\n          <p id=\"modal-desc\" style=\"font-size: 14px; color: #ccc; margin-bottom: 8px; line-height: 1.4;\"></p>\n          <div id=\"modal-link-box\"></div>\n        </div>\n\n        <div class=\"comments-list\" id=\"modal-comments\"></div>\n\n        <div id=\"comment-form\">\n          <input type=\"text\" id=\"new-comment\" placeholder=\"Escreva um comentário...\">\n          <button class=\"btn\" onclick=\"adicionarComentario()\" style=\"width: 100%; margin-top: 5px;\">Comentar</button>\n        </div>\n      </div>\n    </div>\n  </div>\n\n  <script>\n    let db = null;\n    let currentUser = JSON.parse(localStorage.getItem('currentUser')) || null;\n    let isRegisterMode = false;\n    let postSelecionadoId = null;\n    let comunidadeAtiva = \"\"; \n    let modoVisualizacao = 'posts'; // 'posts' ou 'chat'\n\n    let tempAvatarBase64 = null;\n    let tempEditAvatarBase64 = null;\n    let tempGroupIconBase64 = null;\n    let tempPostMedia = null;\n\n    const ADMIN_NICK = \"roxfoxyt\";\n    const DB_NAME = \"FanmadeCommunitiesDB_v5\";\n    const DB_VERSION = 1;\n\n    function isAdmin() {\n      return currentUser && currentUser.username.toLowerCase() === ADMIN_NICK;\n    }\n\n    function comprimirImagem(file, maxWidth, quality, callback) {\n      const reader = new FileReader();\n      reader.onload = function(e) {\n        const img = new Image();\n        img.onload = function() {\n          const canvas = document.createElement('canvas');\n          let width = img.width;\n          let height = img.height;\n\n          if (width > maxWidth) {\n            height = Math.round((height * maxWidth) / width);\n            width = maxWidth;\n          }\n\n          canvas.width = width;\n          canvas.height = height;\n          const ctx = canvas.getContext('2d');\n          ctx.drawImage(img, 0, 0, width, height);\n          callback(canvas.toDataURL('image/jpeg', quality));\n        };\n        img.src = e.target.result;\n      };\n      reader.readAsDataURL(file);\n    }\n\n    const request = indexedDB.open(DB_NAME, DB_VERSION);\n\n    request.onupgradeneeded = (e) => {\n      let dbInstance = e.target.result;\n      if (!dbInstance.objectStoreNames.contains(\"usuarios\")) {\n        dbInstance.createObjectStore(\"usuarios\", { keyPath: \"username\" });\n      }\n      if (!dbInstance.objectStoreNames.contains(\"grupos\")) {\n        dbInstance.createObjectStore(\"grupos\", { keyPath: \"id\", autoIncrement: true });\n      }\n      if (!dbInstance.objectStoreNames.contains(\"posts\")) {\n        dbInstance.createObjectStore(\"posts\", { keyPath: \"id\", autoIncrement: true });\n      }\n    };\n\n    request.onsuccess = (e) => {\n      db = e.target.result;\n      \n      verificarConviteURL();\n\n      if (currentUser) {\n        const tx = db.transaction([\"usuarios\"], \"readonly\");\n        const req = tx.objectStore(\"usuarios\").get(currentUser.username);\n        req.onsuccess = () => {\n          if (req.result) {\n            currentUser = { username: req.result.username, avatar: req.result.avatar, bio: req.result.bio || '' };\n            localStorage.setItem('currentUser', JSON.stringify(currentUser));\n          }\n          updateUI();\n        };\n      } else {\n        updateUI();\n      }\n\n      carregarAbasComunidades();\n      carregarGruposSelect();\n      carregarPosts();\n    };\n\n    request.onerror = (e) => console.error(\"Erro IndexedDB:\", e.target.error);\n\n    function verificarConviteURL() {\n      const urlParams = new URLSearchParams(window.location.search);\n      const token = urlParams.get('join');\n      if (!token) return;\n\n      const checkInterval = setInterval(() => {\n        if (db) {\n          clearInterval(checkInterval);\n          if (!currentUser) {\n            alert(\"Você precisa estar logado para aceitar o convite da comunidade privada!\");\n            return;\n          }\n\n          const tx = db.transaction([\"grupos\"], \"readwrite\");\n          const store = tx.objectStore(\"grupos\");\n          store.openCursor().onsuccess = (e) => {\n            const cursor = e.target.result;\n            if (cursor) {\n              let grupo = cursor.value;\n              if (grupo.inviteToken === token) {\n                if (!grupo.membros) grupo.membros = [grupo.criador];\n                if (!grupo.membros.includes(currentUser.username)) {\n                  grupo.membros.push(currentUser.username);\n                  cursor.update(grupo);\n                  alert(`Você entrou na comunidade privada \"${grupo.nome}\" com sucesso!`);\n                } else {\n                  alert(`Você já faz parte da comunidade \"${grupo.nome}\".`);\n                }\n                window.history.replaceState({}, document.title, window.location.pathname);\n                carregarAbasComunidades();\n                carregarPosts();\n              }\n              cursor.continue();\n            }\n          };\n        }\n      }, 100);\n    }\n\n    function updateUI() {\n      const authHeader = document.getElementById('auth-header');\n      const authPanel = document.getElementById('auth-panel');\n      const uploadPanel = document.getElementById('upload-panel');\n      const adminContainer = document.getElementById('admin-tools-container');\n\n      if (currentUser) {\n        const adminBadgeHtml = isAdmin() ? `<span class=\"admin-badge\">ADMIN</span>` : '';\n        authHeader.innerHTML = `\n          <div class=\"user-profile-badge\" onclick=\"abrirModalPerfil()\" title=\"Clique para editar seu perfil\">\n            <img class=\"avatar-header\" src=\"${currentUser.avatar || 'https://via.placeholder.com/36/333/FFF?text=U'}\" alt=\"Avatar\">\n            <div style=\"text-align: left;\">\n              <strong style=\"display: block; font-size: 14px;\">@${currentUser.username} ${adminBadgeHtml}</strong>\n              <small style=\"color: #ff9800; font-size: 11px;\">✏️ Editar perfil</small>\n            </div>\n          </div>\n          <button class=\"btn btn-danger\" onclick=\"logout()\">Sair</button>\n        `;\n        authPanel.style.display = 'none';\n        uploadPanel.style.display = 'block';\n\n        if (isAdmin()) {\n          adminContainer.style.display = 'block';\n          adminContainer.innerHTML = `\n            <div class=\"admin-panel-box\">\n              <h4 style=\"color: #ff4757; margin-bottom: 8px;\">🛡️ Painel de Administração (@roxfoxyt)</h4>\n              <p style=\"font-size: 12px; color: #aaa; margin-bottom: 10px;\">Você possui privilégios globais para moderar comunidades e apagar postagens inadequadas.</p>\n              <button class=\"btn btn-danger\" style=\"font-size: 12px; padding: 5px 10px;\" onclick=\"adminLimparBanco()\">⚠️ Resetar Todos os Dados</button>\n            </div>\n          `;\n        } else {\n          adminContainer.style.display = 'none';\n        }\n      } else {\n        authHeader.innerHTML = `<span>Você está desconectado.</span>`;\n        authPanel.style.display = 'block';\n        uploadPanel.style.display = 'none';\n        adminContainer.style.display = 'none';\n      }\n    }\n\n    function toggleAuthMode() {\n      isRegisterMode = !isRegisterMode;\n      document.getElementById('auth-title').innerText = isRegisterMode ? 'Criar Conta no Fanmade' : 'Entrar no Fanmade Communities';\n      document.getElementById('btn-submit').innerText = isRegisterMode ? 'Cadastrar e Entrar' : 'Entrar';\n      document.getElementById('auth-link-text').innerText = isRegisterMode ? 'Já tem uma conta?' : 'Não tem conta?';\n      document.getElementById('auth-link').innerText = isRegisterMode ? 'Faça Login' : 'Cadastre-se';\n      document.getElementById('avatar-field').style.display = isRegisterMode ? 'block' : 'none';\n      document.getElementById('reg-recovery-code').style.display = isRegisterMode ? 'block' : 'none';\n      document.getElementById('forgot-link-box').style.display = isRegisterMode ? 'none' : 'block';\n      document.getElementById('auth-msg').innerText = '';\n    }\n\n    function mudarAbaPainel(aba) {\n      document.getElementById('tab-post').classList.toggle('active', aba === 'post');\n      document.getElementById('tab-grupo').classList.toggle('active', aba === 'grupo');\n      document.getElementById('form-post').style.display = aba === 'post' ? 'block' : 'none';\n      document.getElementById('form-grupo').style.display = aba === 'grupo' ? 'block' : 'none';\n      document.getElementById('action-msg').innerText = '';\n    }\n\n    function alternarModo(modo) {\n      modoVisualizacao = modo;\n      document.getElementById('btn-mode-posts').className = modo === 'posts' ? 'btn' : 'btn btn-secondary';\n      document.getElementById('btn-mode-chat').className = modo === 'chat' ? 'btn' : 'btn btn-secondary';\n      \n      document.getElementById('galeria').style.display = modo === 'posts' ? 'grid' : 'none';\n      document.getElementById('chat-box').style.display = modo === 'chat' ? 'flex' : 'none';\n\n      if (modo === 'posts') {\n        carregarPosts();\n      } else {\n        if (!comunidadeAtiva) {\n          alert(\"Selecione uma comunidade específica para abrir o chat de grupo.\");\n          alternarModo('posts');\n          return;\n        }\n        carregarMensagensChat();\n      }\n    }\n\n    function carregarPreviewAvatar(e, targetImgId, stateType) {\n      const file = e.target.files[0];\n      if (!file) return;\n      comprimirImagem(file, 200, 0.7, (base64) => {\n        if (stateType === 'tempRegister') tempAvatarBase64 = base64;\n        if (stateType === 'tempEdit') tempEditAvatarBase64 = base64;\n        document.getElementById(targetImgId).src = base64;\n      });\n    }\n\n    function carregarPreviewGrupo(e) {\n      const file = e.target.files[0];\n      if (!file) return;\n      comprimirImagem(file, 200, 0.7, (base64) => {\n        tempGroupIconBase64 = base64;\n        document.getElementById('group-icon-preview').src = base64;\n      });\n    }\n\n    function handleAuth() {\n      const u = document.getElementById('username').value.trim().toLowerCase();\n      const p = document.getElementById('password').value.trim();\n      const recCode = document.getElementById('reg-recovery-code').value.trim();\n      const bio = document.getElementById('reg-bio').value.trim();\n      const msg = document.getElementById('auth-msg');\n      msg.innerText = '';\n\n      if (!u || !p) {\n        msg.style.color = '#f44336';\n        msg.innerText = 'Preencha usuário e senha!';\n        return;\n      }\n\n      if (isRegisterMode && !recCode) {\n        msg.style.color = '#f44336';\n        msg.innerText = 'Defina uma Senha Única de recuperação!';\n        return;\n      }\n\n      if (!db) return;\n\n      const tx = db.transaction([\"usuarios\"], \"readwrite\");\n      const store = tx.objectStore(\"usuarios\");\n      const req = store.get(u);\n\n      req.onsuccess = () => {\n        const user = req.result;\n\n        if (isRegisterMode) {\n          if (user) {\n            msg.style.color = '#f44336';\n            msg.innerText = 'Este nome de usuário já existe!';\n          } else {\n            const avatarFinal = tempAvatarBase64 || 'https://via.placeholder.com/60/333/FFF?text=' + u.charAt(0).toUpperCase();\n            const novoUsuario = { username: u, password: p, recoveryCode: recCode, avatar: avatarFinal, bio: bio };\n            \n            store.add(novoUsuario).onsuccess = () => {\n              currentUser = { username: u, avatar: avatarFinal, bio: bio };\n              localStorage.setItem('currentUser', JSON.stringify(currentUser));\n              document.getElementById('username').value = '';\n              document.getElementById('password').value = '';\n              document.getElementById('reg-recovery-code').value = '';\n              tempAvatarBase64 = null;\n              updateUI();\n            };\n          }\n        } else {\n          if (user && user.password === p) {\n            currentUser = { username: user.username, avatar: user.avatar, bio: user.bio || '' };\n            localStorage.setItem('currentUser', JSON.stringify(currentUser));\n            document.getElementById('username').value = '';\n            document.getElementById('password').value = '';\n            updateUI();\n          } else {\n            msg.style.color = '#f44336';\n            msg.innerText = 'Usuário ou senha incorretos!';\n          }\n        }\n      };\n    }\n\n    function abrirModalRecuperacao() {\n      document.getElementById('rec-username').value = '';\n      document.getElementById('rec-code').value = '';\n      document.getElementById('rec-new-pass').value = '';\n      document.getElementById('rec-msg').innerText = '';\n      document.getElementById('modal-recuperacao').style.display = 'flex';\n    }\n\n    function fecharModalRecuperacao(e) {\n      if (e.target.id === 'modal-recuperacao') document.getElementById('modal-recuperacao').style.display = 'none';\n    }\n\n    function recuperarConta() {\n      const u = document.getElementById('rec-username').value.trim().toLowerCase();\n      const code = document.getElementById('rec-code').value.trim();\n      const newPass = document.getElementById('rec-new-pass').value.trim();\n      const msg = document.getElementById('rec-msg');\n\n      if (!u || !code || !newPass) {\n        msg.style.color = '#f44336';\n        msg.innerText = 'Preencha todos os campos!';\n        return;\n      }\n\n      if (!db) return;\n\n      const tx = db.transaction([\"usuarios\"], \"readwrite\");\n      const store = tx.objectStore(\"usuarios\");\n      const req = store.get(u);\n\n      req.onsuccess = () => {\n        const user = req.result;\n        if (user && user.recoveryCode === code) {\n          user.password = newPass;\n          store.put(user);\n          msg.style.color = '#28a745';\n          msg.innerText = 'Senha alterada! Feche essa janela e faça login.';\n        } else {\n          msg.style.color = '#f44336';\n          msg.innerText = 'Usuário ou Senha Única inválida!';\n        }\n      };\n    }\n\n    function logout() {\n      currentUser = null;\n      localStorage.removeItem('currentUser');\n      updateUI();\n    }\n\n    function abrirModalPerfil() {\n      if (!currentUser) return;\n      document.getElementById('edit-username').value = currentUser.username;\n      document.getElementById('edit-bio').value = currentUser.bio || '';\n      document.getElementById('edit-avatar-preview').src = currentUser.avatar || 'https://via.placeholder.com/60/333/FFF?text=U';\n      document.getElementById('edit-profile-msg').innerText = '';\n      tempEditAvatarBase64 = null;\n      document.getElementById('modal-perfil').style.display = 'flex';\n    }\n\n    function salvarPerfil() {\n      if (!db) return;\n      const newUsername = document.getElementById('edit-username').value.trim().toLowerCase();\n      const newBio = document.getElementById('edit-bio').value.trim();\n      const msg = document.getElementById('edit-profile-msg');\n      msg.innerText = '';\n\n      if (!newUsername) {\n        msg.style.color = '#f44336';\n        msg.innerText = 'O nome de usuário não pode ficar em branco!';\n        return;\n      }\n\n      if (newUsername === ADMIN_NICK && currentUser.username !== ADMIN_NICK) {\n        msg.style.color = '#f44336';\n        msg.innerText = 'Este nickname é reservado para o Administrador!';\n        return;\n      }\n\n      const oldUsername = currentUser.username;\n      const newAvatar = tempEditAvatarBase64 || currentUser.avatar;\n\n      const tx = db.transaction([\"usuarios\", \"posts\", \"grupos\"], \"readwrite\");\n      const userStore = tx.objectStore(\"usuarios\");\n      const postStore = tx.objectStore(\"posts\");\n      const grupoStore = tx.objectStore(\"grupos\");\n\n      userStore.get(newUsername).onsuccess = (e) => {\n        if (newUsername !== oldUsername && e.target.result) {\n          msg.style.color = '#f44336';\n          msg.innerText = 'Esse handle já está em uso!';\n          return;\n        }\n\n        userStore.get(oldUsername).onsuccess = (evt) => {\n          const userData = evt.target.result;\n          if (newUsername !== oldUsername) userStore.delete(oldUsername);\n\n          userStore.put({\n            username: newUsername,\n            password: userData ? userData.password : '',\n            recoveryCode: userData ? userData.recoveryCode : '',\n            avatar: newAvatar,\n            bio: newBio\n          });\n        };\n\n        postStore.openCursor().onsuccess = (evt) => {\n          const cursor = evt.target.result;\n          if (cursor) {\n            let post = cursor.value;\n            let mudou = false;\n\n            if (post.autor === oldUsername) {\n              post.autor = newUsername;\n              post.autorAvatar = newAvatar;\n              post.autorBio = newBio;\n              mudou = true;\n            }\n\n            if (post.comentarios && post.comentarios.length > 0) {\n              post.comentarios.forEach(c => {\n                if (c.autor === oldUsername) {\n                  c.autor = newUsername;\n                  c.autorAvatar = newAvatar;\n                  mudou = true;\n                }\n              });\n            }\n\n            if (mudou) cursor.update(post);\n            cursor.continue();\n          }\n        };\n\n        grupoStore.openCursor().onsuccess = (evt) => {\n          const cursor = evt.target.result;\n          if (cursor) {\n            let g = cursor.value;\n            let mudouGrupo = false;\n            if (g.criador === oldUsername) {\n              g.criador = newUsername;\n              mudouGrupo = true;\n            }\n            if (g.membros && g.membros.includes(oldUsername)) {\n              g.membros = g.membros.map(m => m === oldUsername ? newUsername : m);\n              mudouGrupo = true;\n            }\n            if (g.mensagens && g.mensagens.length > 0) {\n              g.mensagens.forEach(m => {\n                if (m.autor === oldUsername) {\n                  m.autor = newUsername;\n                  m.avatar = newAvatar;\n                  mudouGrupo = true;\n                }\n              });\n            }\n            if (mudouGrupo) cursor.update(g);\n            cursor.continue();\n          }\n        };\n\n        tx.oncomplete = () => {\n          currentUser = { username: newUsername, avatar: newAvatar, bio: newBio };\n          localStorage.setItem('currentUser', JSON.stringify(currentUser));\n          updateUI();\n          carregarAbasComunidades();\n          carregarPosts();\n          document.getElementById('modal-perfil').style.display = 'none';\n        };\n      };\n    }\n\n    function fecharModalPerfil(e) {\n      if (e.target.id === 'modal-perfil') document.getElementById('modal-perfil').style.display = 'none';\n    }\n\n    function validarSelecaoMidia(event) {\n      const file = event.target.files[0];\n      const status = document.getElementById('media-nome-status');\n      if (!file) return;\n\n      status.style.color = '#ff9800';\n      status.innerText = 'Processando arquivo...';\n\n      if (file.type.startsWith('image/')) {\n        comprimirImagem(file, 900, 0.75, (base64) => {\n          tempPostMedia = { type: 'image', data: base64 };\n          status.style.color = '#28a745';\n          status.innerText = '✓ Imagem Pronta!';\n        });\n      } else if (file.type.startsWith('video/')) {\n        const reader = new FileReader();\n        reader.onload = function(e) {\n          tempPostMedia = { type: 'video', data: e.target.result };\n          status.style.color = '#28a745';\n          status.innerText = '✓ Vídeo Pronto!';\n        };\n        reader.readAsDataURL(file);\n      } else {\n        status.style.color = '#f44336';\n        status.innerText = 'Formato não suportado!';\n      }\n    }\n\n    function salvarGrupo() {\n      if (!db) return;\n      const name = document.getElementById('group-name').value.trim();\n      const desc = document.getElementById('group-desc').value.trim();\n      const isPrivate = document.getElementById('group-is-private').checked;\n      const msg = document.getElementById('action-msg');\n      msg.innerText = '';\n\n      if (!name) {\n        msg.style.color = '#f44336';\n        msg.innerText = 'Digite o nome da comunidade!';\n        return;\n      }\n\n      const tx = db.transaction([\"grupos\"], \"readwrite\");\n      const store = tx.objectStore(\"grupos\");\n      \n      store.add({\n        nome: name,\n        descricao: desc,\n        icone: tempGroupIconBase64 || 'https://via.placeholder.com/60/333/FFF?text=C',\n        criador: currentUser.username,\n        isPrivate: isPrivate,\n        membros: [currentUser.username],\n        inviteToken: isPrivate ? Math.random().toString(36).substr(2, 9) : null,\n        mensagens: []\n      });\n\n      tx.oncomplete = () => {\n        msg.style.color = '#28a745';\n        msg.innerText = 'Comunidade criada com sucesso!';\n        document.getElementById('group-name').value = '';\n        document.getElementById('group-desc').value = '';\n        document.getElementById('group-is-private').checked = false;\n        tempGroupIconBase64 = null;\n        carregarGruposSelect();\n        carregarAbasComunidades();\n      };\n    }\n\n    function carregarGruposSelect() {\n      if (!db) return;\n      const select = document.getElementById('post-group-select');\n      select.innerHTML = '<option value=\"\">Feed Geral</option>';\n\n      const tx = db.transaction([\"grupos\"], \"readonly\");\n      tx.objectStore(\"grupos\").getAll().onsuccess = (e) => {\n        const grupos = e.target.result;\n        grupos.forEach(g => {\n          if (!g.isPrivate || (currentUser && (g.membros.includes(currentUser.username) || isAdmin()))) {\n            const prefixo = g.isPrivate ? '🔒 ' : '👥 ';\n            select.innerHTML += `<option value=\"${g.nome}\">${prefixo}${g.nome}</option>`;\n          }\n        });\n      };\n    }\n\n    function carregarAbasComunidades() {\n      if (!db) return;\n      const navBar = document.getElementById('communities-nav-bar');\n      \n      const tx = db.transaction([\"grupos\"], \"readonly\");\n      tx.objectStore(\"grupos\").getAll().onsuccess = (e) => {\n        const grupos = e.target.result;\n        \n        let html = `<div class=\"community-tab ${comunidadeAtiva === '' ? 'active' : ''}\" onclick=\"filtrarComunidade('')\">🔥 Feed Geral</div>`;\n        \n        grupos.forEach(g => {\n          const isPrivate = g.isPrivate;\n          const icone = isPrivate ? '🔒 ' : '👥 ';\n          const isActive = comunidadeAtiva === g.nome ? 'active' : '';\n          \n          let inviteButtonHtml = '';\n          if (isPrivate && currentUser && (g.criador === currentUser.username || isAdmin())) {\n            inviteButtonHtml = ` <span onclick=\"event.stopPropagation(); copiarLinkConvite('${g.inviteToken}')\" style=\"color:#00a2ff; font-size:11px; margin-left:4px;\" title=\"Copiar Link de Convite\">🔗</span>`;\n          }\n\n          let deleteAdminBtn = isAdmin() ? ` <span onclick=\"event.stopPropagation(); adminDeletarGrupo('${g.nome}')\" style=\"color:#ff4757; font-weight:bold; margin-left:4px;\" title=\"Deletar Comunidade\">×</span>` : '';\n          \n          html += `<div class=\"community-tab ${isActive}\" onclick=\"clicarAbaComunidade('${g.nome}')\">${icone}${g.nome}${inviteButtonHtml}${deleteAdminBtn}</div>`;\n        });\n\n        navBar.innerHTML = html;\n      };\n    }\n\n    function clicarAbaComunidade(nomeGrupo) {\n      if (!db) return;\n      const tx = db.transaction([\"grupos\"], \"readonly\");\n      tx.objectStore(\"grupos\").getAll().onsuccess = (e) => {\n        const grupo = e.target.result.find(g => g.nome === nomeGrupo);\n        if (grupo && grupo.isPrivate) {\n          const podeAcessar = currentUser && (grupo.membros.includes(currentUser.username) || isAdmin());\n          if (!podeAcessar) {\n            alert(\"Esta comunidade é privada! Você precisa de um link de convite válido para entrar.\");\n            return;\n          }\n        }\n        filtrarComunidade(nomeGrupo);\n      };\n    }\n\n    function copiarLinkConvite(token) {\n      const link = `${window.location.origin}${window.location.pathname}?join=${token}`;\n      prompt(\"Copie este link de convite exclusivo para enviar aos membros:\", link);\n    }\n\n    function filtrarComunidade(nomeGrupo) {\n      comunidadeAtiva = nomeGrupo;\n      document.getElementById('current-community-title').innerText = nomeGrupo ? `Comunidade: ${nomeGrupo}` : 'Feed Geral';\n      \n      // Se o feed geral estiver selecionado, força a visão de posts pois feed geral não tem chat de grupo específico\n      if (nomeGrupo === \"\" && modoVisualizacao === 'chat') {\n        alternarModo('posts');\n      }\n\n      carregarAbasComunidades();\n      if (modoVisualizacao === 'posts') {\n        carregarPosts();\n      } else {\n        carregarMensagensChat();\n      }\n    }\n\n    function enviarMensagemChat() {\n      if (!currentUser || !comunidadeAtiva) {\n        alert(\"Selecione uma comunidade para enviar mensagens no chat.\");\n        return;\n      }\n      const input = document.getElementById('chat-input');\n      const texto = input.value.trim();\n      if (!texto) return;\n\n      const tx = db.transaction([\"grupos\"], \"readwrite\");\n      const store = tx.objectStore(\"grupos\");\n      \n      store.openCursor().onsuccess = (e) => {\n        const cursor = e.target.result;\n        if (cursor) {\n          let g = cursor.value;\n          if (g.nome === comunidadeAtiva) {\n            if (!g.mensagens) g.mensagens = [];\n            g.mensagens.push({\n              autor: currentUser.username,\n              avatar: currentUser.avatar,\n              texto: texto,\n              data: new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })\n            });\n            cursor.update(g);\n            input.value = '';\n            carregarMensagensChat();\n          }\n          cursor.continue();\n        }\n      };\n    }\n\n    function carregarMensagensChat() {\n      if (!comunidadeAtiva) return;\n      const container = document.getElementById('chat-messages');\n      container.innerHTML = '<p style=\"color:#777; text-align:center;\">Carregando chat...</p>';\n\n      const tx = db.transaction([\"grupos\"], \"readonly\");\n      tx.objectStore(\"grupos\").getAll().onsuccess = (e) => {\n        const grupo = e.target.result.find(g => g.nome === comunidadeAtiva);\n        container.innerHTML = '';\n        \n        if (grupo && grupo.mensagens && grupo.mensagens.length > 0) {\n          grupo.mensagens.forEach(msg => {\n            const isMe = currentUser && msg.autor === currentUser.username;\n            const isMsgAdmin = msg.autor.toLowerCase() === ADMIN_NICK;\n            const adminBadgeHtml = isMsgAdmin ? `<span class=\"admin-badge\">ADMIN</span>` : '';\n\n            const div = document.createElement('div');\n            div.className = `chat-msg ${isMe ? 'me' : ''}`;\n            div.innerHTML = `\n              <div class=\"chat-msg-header\">\n                <img class=\"chat-msg-avatar\" src=\"${msg.avatar || 'https://via.placeholder.com/20/333/FFF?text=U'}\" alt=\"Avatar\">\n                <strong style=\"font-size:12px;\">@${msg.autor}</strong> ${adminBadgeHtml}\n                <span style=\"font-size:10px; opacity:0.6; margin-left:auto;\">${msg.data || ''}</span>\n              </div>\n              <div style=\"word-break: break-word;\">${msg.texto}</div>\n            `;\n            container.appendChild(div);\n          });\n          container.scrollTop = container.scrollHeight;\n        } else {\n          container.innerHTML = '<p style=\"text-align:center; color:#555; margin-top:20px;\">Nenhuma mensagem nesta comunidade ainda. Seja o primeiro a falar!</p>';\n        }\n      };\n    }\n\n    function salvarPost() {\n      if (!db) return;\n      const msg = document.getElementById('action-msg');\n      msg.innerText = '';\n\n      if (!tempPostMedia) {\n        msg.style.color = '#f44336';\n        msg.innerText = 'Selecione uma imagem ou vídeo!';\n        return;\n      }\n\n      const grupo = document.getElementById('post-group-select').value;\n      const title = document.getElementById('post-title').value.trim();\n      const desc = document.getElementById('post-desc').value.trim();\n      let link = document.getElementById('post-link').value.trim();\n\n      if (link && !link.startsWith('http://') && !link.startsWith('https://')) {\n        link = 'https://' + link;\n      }\n\n      const tx = db.transaction([\"posts\"], \"readwrite\");\n      const store = tx.objectStore(\"posts\");\n      \n      store.add({\n        mediaType: tempPostMedia.type,\n        mediaData: tempPostMedia.data,\n        grupo: grupo || null,\n        autor: currentUser.username,\n        autorAvatar: currentUser.avatar,\n        autorBio: currentUser.bio || '',\n        titulo: title || 'Sem título',\n        descricao: desc,\n        link: link,\n        dataCriacao: new Date().toLocaleString('pt-BR'),\n        comentarios: []\n      });\n\n      tx.oncomplete = () => {\n        msg.style.color = '#28a745';\n        msg.innerText = 'Post publicado com sucesso!';\n        document.getElementById('mediaInput').value = '';\n        document.getElementById('post-title').value = '';\n        document.getElementById('post-desc').value = '';\n        document.getElementById('post-link').value = '';\n        document.getElementById('media-nome-status').innerText = '';\n        tempPostMedia = null;\n        carregarPosts();\n      };\n    }\n\n    function carregarPosts() {\n      if (!db) return;\n      const galeria = document.getElementById('galeria');\n      galeria.innerHTML = '';\n\n      const tx = db.transaction([\"posts\", \"grupos\"], \"readonly\");\n      const grupoStore = tx.objectStore(\"grupos\");\n      \n      grupoStore.getAll().onsuccess = (eGrupos) => {\n        const grupos = eGrupos.target.result;\n\n        tx.objectStore(\"posts\").getAll().onsuccess = (ePosts) => {\n          let posts = ePosts.target.result.reverse();\n\n          posts = posts.filter(post => {\n            if (!post.grupo) return true;\n            const grupoInfo = grupos.find(g => g.nome === post.grupo);\n            if (!grupoInfo) return true;\n\n            if (grupoInfo.isPrivate) {\n              const temAcesso = currentUser && (grupoInfo.membros && grupoInfo.membros.includes(currentUser.username) || isAdmin());\n              return temAcesso;\n            }\n            return true;\n          });\n\n          if (comunidadeAtiva !== \"\") {\n            posts = posts.filter(p => p.grupo === comunidadeAtiva);\n          }\n          \n          if (posts.length === 0) {\n            galeria.innerHTML = '<p style=\"color:#888;\">Nenhuma publicação encontrada nesta aba.</p>';\n            return;\n          }\n\n          posts.forEach(post => {\n            const card = document.createElement('div');\n            card.className = 'post-card';\n            card.onclick = () => abrirModalPost(post.id);\n            \n            let mediaElement = post.mediaType === 'video'\n              ? `<video src=\"${post.mediaData}\" muted preload=\"metadata\"></video>`\n              : `<img src=\"${post.mediaData}\" alt=\"Mídia\">`;\n\n            const isPostAuthorAdmin = post.autor.toLowerCase() === ADMIN_NICK;\n            const authorAdminBadge = isPostAuthorAdmin ? `<span class=\"admin-badge\">ADMIN</span>` : '';\n            \n            let deleteBtnHtml = '';\n            if (currentUser && (currentUser.username === post.autor || isAdmin())) {\n              deleteBtnHtml = `<button class=\"btn btn-danger\" style=\"font-size:10px; padding:2px 6px;\" onclick=\"event.stopPropagation(); deletarPost(${post.id})\">Apagar</button>`;\n            }\n\n            card.innerHTML = `\n              ${mediaElement}\n              <div class=\"post-meta\">\n                ${post.grupo ? `<span class=\"group-badge\">👥 ${post.grupo}</span>` : ''}\n                <div class=\"post-author-box\">\n                  <div class=\"author-info-left\">\n                    <img class=\"post-author-avatar\" src=\"${post.autorAvatar || 'https://via.placeholder.com/25/333/FFF?text=U'}\" alt=\"Avatar\">\n                    <strong>@${post.autor}</strong> ${authorAdminBadge}\n                  </div>\n                  ${deleteBtnHtml}\n                </div>\n                <div class=\"post-title\">${post.titulo}</div>\n                ${post.descricao ? `<div class=\"post-desc\">${post.descricao}</div>` : ''}\n                ${post.link ? `<a class=\"post-link\" href=\"${post.link}\" target=\"_blank\" onclick=\"event.stopPropagation()\">🔗 ${post.link}</a>` : ''}\n                <small style=\"margin-top:auto; padding-top:6px; color:#777;\">💬 ${post.comentarios ? post.comentarios.length : 0} comentários</small>\n              </div>\n            `;\n            galeria.appendChild(card);\n          });\n        };\n      };\n    }\n\n    function deletarPost(postId) {\n      if (!confirm(\"Tem certeza que deseja apagar esta publicação?\")) return;\n      const tx = db.transaction([\"posts\"], \"readwrite\");\n      tx.objectStore(\"posts\").delete(postId);\n      tx.oncomplete = () => {\n        carregarPosts();\n        document.getElementById('modal-post').style.display = 'none';\n      };\n    }\n\n    function adminDeletarGrupo(nomeGrupo) {\n      if (!isAdmin()) return;\n      if (!confirm(`ADMIN: Deseja realmente deletar a comunidade \"${nomeGrupo}\"?`)) return;\n\n      const tx = db.transaction([\"grupos\", \"posts\"], \"readwrite\");\n      const grupoStore = tx.objectStore(\"grupos\");\n      const postStore = tx.objectStore(\"posts\");\n\n      grupoStore.openCursor().onsuccess = (e) => {\n        const cursor = e.target.result;\n        if (cursor) {\n          if (cursor.value.nome === nomeGrupo) cursor.delete();\n          cursor.continue();\n        }\n      };\n\n      postStore.openCursor().onsuccess = (e) => {\n        const cursor = e.target.result;\n        if (cursor) {\n          let p = cursor.value;\n          if (p.grupo === nomeGrupo) {\n            p.grupo = null;\n            cursor.update(p);\n          }\n          cursor.continue();\n        }\n      };\n\n      tx.oncomplete = () => {\n        if (comunidadeAtiva === nomeGrupo) {\n          comunidadeAtiva = \"\";\n          alternarModo('posts');\n        }\n        carregarAbasComunidades();\n        carregarGruposSelect();\n        carregarPosts();\n      };\n    }\n\n    function adminLimparBanco() {\n      if (!isAdmin()) return;\n      if (!confirm(\"ATENÇÃO ADMIN: Deseja apagar todos os posts e comunidades do sistema?\")) return;\n      \n      const tx = db.transaction([\"posts\", \"grupos\"], \"readwrite\");\n      tx.objectStore(\"posts\").clear();\n      tx.objectStore(\"grupos\").clear();\n\n      tx.oncomplete = () => {\n        comunidadeAtiva = \"\";\n        alternarModo('posts');\n        carregarAbasComunidades();\n        carregarGruposSelect();\n        carregarPosts();\n        alert(\"Sistema limpo com sucesso!\");\n      };\n    }\n\n    function abrirModalPost(postId) {\n      if (!db) return;\n      postSelecionadoId = postId;\n      const tx = db.transaction([\"posts\"], \"readonly\");\n      const store = tx.objectStore(\"posts\");\n      const req = store.get(postId);\n\n      req.onsuccess = () => {\n        const post = req.result;\n        const container = document.getElementById('modal-media-container');\n        \n        if (post.mediaType === 'video') {\n          container.innerHTML = `<video src=\"${post.mediaData}\" controls autoplay style=\"max-width:100%; max-height:70vh;\"></video>`;\n        } else {\n          container.innerHTML = `<img src=\"${post.mediaData}\" alt=\"Mídia Ampliada\">`;\n        }\n\n        document.getElementById('modal-group-badge').innerHTML = post.grupo \n          ? `<span class=\"group-badge\" style=\"font-size:12px; margin-bottom:8px;\">👥 Comunidade: ${post.grupo}</span>` \n          : '';\n\n        document.getElementById('modal-author-avatar').src = post.autorAvatar || 'https://via.placeholder.com/40/333/FFF?text=U';\n        document.getElementById('modal-author-name').innerText = `@${post.autor}`;\n        document.getElementById('modal-author-bio').innerText = post.autorBio ? `\"${post.autorBio}\"` : '';\n        document.getElementById('modal-date').innerText = `Publicado em: ${post.dataCriacao}`;\n        \n        document.getElementById('modal-title').innerText = post.titulo;\n        document.getElementById('modal-desc').innerText = post.descricao || 'Sem legenda.';\n        \n        const linkBox = document.getElementById('modal-link-box');\n        linkBox.innerHTML = post.link ? `<a class=\"post-link\" href=\"${post.link}\" target=\"_blank\" style=\"font-size:13px;\">🔗 ${post.link}</a>` : '';\n        \n        renderizarComentarios(post.comentarios || []);\n        document.getElementById('comment-form').style.display = currentUser ? 'block' : 'none';\n        document.getElementById('modal-post').style.display = 'flex';\n      };\n    }\n\n    function renderizarComentarios(comentarios) {\n      const container = document.getElementById('modal-comments');\n      if (comentarios.length === 0) {\n        container.innerHTML = '<p style=\"color:#666; font-size:13px;\">Nenhum comentário ainda.</p>';\n        return;\n      }\n\n      container.innerHTML = comentarios.map((c, index) => {\n        const isCommentAuthorAdmin = c.autor.toLowerCase() === ADMIN_NICK;\n        const cAdminBadge = isCommentAuthorAdmin ? `<span class=\"admin-badge\">ADMIN</span>` : '';\n        \n        let deleteCommentBtn = '';\n        if (currentUser && (currentUser.username === c.autor || isAdmin())) {\n          deleteCommentBtn = `<span onclick=\"deletarComentario(${index})\" style=\"color:#ff4757; cursor:pointer; font-size:11px;\" title=\"Apagar\">❌</span>`;\n        }\n\n        return `\n          <div class=\"comment-item\">\n            <div style=\"display:flex; align-items:flex-start; gap:8px;\">\n              <img class=\"comment-avatar\" src=\"${c.autorAvatar || 'https://via.placeholder.com/22/333/FFF?text=U'}\" alt=\"Avatar\">\n              <div>\n                <strong>@${c.autor} ${cAdminBadge}:</strong> <span>${c.texto}</span>\n              </div>\n            </div>\n            ${deleteCommentBtn}\n          </div>\n        `;\n      }).join('');\n    }\n\n    function adicionarComentario() {\n      if (!currentUser || !postSelecionadoId || !db) return;\n\n      const input = document.getElementById('new-comment');\n      const texto = input.value.trim();\n      if (!texto) return;\n\n      const tx = db.transaction([\"posts\"], \"readwrite\");\n      const store = tx.objectStore(\"posts\");\n      const req = store.get(postSelecionadoId);\n\n      req.onsuccess = () => {\n        const post = req.result;\n        if (!post.comentarios) post.comentarios = [];\n\n        post.comentarios.push({\n          autor: currentUser.username,\n          autorAvatar: currentUser.avatar,\n          texto: texto,\n          data: new Date().toLocaleTimeString()\n        });\n\n        store.put(post);\n\n        tx.oncomplete = () => {\n          input.value = '';\n          renderizarComentarios(post.comentarios);\n          carregarPosts();\n        };\n      };\n    }\n\n    function deletarComentario(index) {\n      if (!db || !postSelecionadoId) return;\n      const tx = db.transaction([\"posts\"], \"readwrite\");\n      const store = tx.objectStore(\"posts\");\n      const req = store.get(postSelecionadoId);\n\n      req.onsuccess = () => {\n        const post = req.result;\n        if (post && post.comentarios) {\n          post.comentarios.splice(index, 1);\n          store.put(post);\n          tx.oncomplete = () => {\n            renderizarComentarios(post.comentarios);\n            carregarPosts();\n          };\n        }\n      };\n    }\n\n    function fecharModalPost(e) {\n      if (e.target.id === 'modal-post') {\n        const container = document.getElementById('modal-media-container');\n        container.innerHTML = '';\n        document.getElementById('modal-post').style.display = 'none';\n        postSelecionadoId = null;\n      }\n    }\n  </script>\n</body>\n</html>\n\n\n"
    }
  ]
}